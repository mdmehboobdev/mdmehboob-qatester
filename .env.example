# Copy this file to ".env" and fill in real values.
# ".env" should never be committed to version control.

# --- App settings ---
APP_ENV=development
# Path to the company config file. Defaults to config/company.json if unset.
COMPANY_CONFIG_PATH=config/company.json

# --- Reserved for a future milestone (AI model integration) ---
# GEMINI_API_KEY=your-api-key-here
